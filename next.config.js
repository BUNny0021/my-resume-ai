// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // For a basic setup, this can be empty.
  // For example:
  // reactStrictMode: true,
};

module.exports = nextConfig;